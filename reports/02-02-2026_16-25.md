# Relatório de Sessão - 02/02/2026 16:25

## O que foi feito

- Configurada a seção `preview` no arquivo `vite.config.ts` do site.
- Adicionado o host permitido `one-configurator4-all.purecore.codes` para o servidor de preview do Vite.
- Atualizado o `CHANGELOG.md` com as novas configurações.

## Por que foi feito

- Para permitir que o servidor de preview do Vite aceite conexões partindo do domínio customizado especificado pelo usuário.
